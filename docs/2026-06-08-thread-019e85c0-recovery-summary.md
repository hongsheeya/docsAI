# Codex Thread Recovery Summary

작성일: 2026-06-08

복원 대상 스레드:
- `019e85c0-692c-7b30-bdc6-691d6e4fddf7`

## 재개 실패 원인

Codex App이 세션을 재개할 때 요청 경로와 활성 경로를 문자열 그대로 비교하면서 실패했다.

- 요청 경로: `/root/.codex/sessions/2026/06/02/rollout-2026-06-02T00-34-00-019e85c0-692c-7b30-bdc6-691d6e4fddf7.jsonl`
- 활성 경로: `/mnt/data/root/.codex/sessions/2026/06/02/rollout-2026-06-02T00-34-00-019e85c0-692c-7b30-bdc6-691d6e4fddf7.jsonl`

현재 서버에서 `/root`는 `/mnt/data/root`를 가리키는 심볼릭 링크다. 따라서 세션 파일 자체는 사라진 것이 아니고, 같은 파일이 두 경로로 보이는 상태다. 앱 내부의 stale-path 판정이 canonical path를 기준으로 하지 않아 재개가 막힌 것으로 판단한다.

## 프로젝트 구조와 작업 원칙

- Codex App 루트: `/opt/app`
- 실제 프로젝트 루트: `/opt/app/project/main`
- 데이터 저장 영역: `/opt/app/datasets`
- 모델/학습 산출물: `/opt/app/storage/training/fall-detection` 및 프로젝트 내부 mirror 경로
- `/opt/app/project/main` 바깥의 데이터/백업 영역은 코드 수정 대상이 아니며, 삭제/이동 없이 사용했다.

## 데이터 수집 흐름

### 낙상/비낙상

- AI-Hub 71641 낙상 행동 데이터 다운로드를 진행했다.
- 초기에 파일키를 `551137`, `551138`로 잘못 잡았고, 이후 `531137`, `531138` 및 `531131`~`531136` 흐름으로 정정했다.
- 저장 위치는 `/opt/app/datasets/fall_classification/aihubs_71641`로 유지했다.

### 행동분류

- 행동 클래스는 `stand`, `walk`, `run`, `sit`, `lie` 5-class로 정했다.
- AI-Hub 71461 행동 분류/상호작용 인식용 한국형 비전 데이터를 검토했고, 파일키 `485408`~`485411`, 추가 `485405`, `485407` 확보를 진행했다.
- AI-Hub 61 사람동작 2020 데이터를 행동분류 보강용으로 사용했다.
- AI-Hub 62 사람동작 영상 데이터는 일부 테스트/검토했지만 최종 핵심 행동 모델에는 사실상 반영하지 않는 방향으로 정리했다.

### 표정/상태

- AI-Hub 82 한국인 감정/표정 데이터 기반 표정 보조 모델을 만들었다.
- AI-Hub 173 운전자 상태 데이터 기반 상태 보조 모델을 만들었다.
- 사용자가 업로드한 `03.AI모델.zip` 외부 표정 모델을 분석했고, 즉시 운영 교체는 보류했다.
- 보류 사유: Docker/runtime 제약, EfficientNet-b5 전처리 adapter 필요, 출력 JSON 정규화 필요, 운영 검증 기준이 일반 7-class accuracy와 다르기 때문.

## 모델 현황

현재 대시보드에 연결된 주요 모델은 다음과 같다.

- RF-Fall v2: 1,592 samples, F1 약 93.0%, Accuracy 약 92.8%, Recall 약 95.4%
- XG-Posture: 8,181 windows, `extra_trees_balanced`, grouped 기준 F1/Accuracy 약 94.3%
- AI-Hub 82 표정: 13,200 samples, F1/Accuracy 약 85.9%
- AI-Hub 173 상태: 47,144 samples, F1 약 90.5%, Accuracy 약 90.2%

목표는 모든 주요 모델 F1 0.93 이상이지만, 표정 82와 상태 173은 운영 보조 모델 성격이므로 단독 F1보다 다음 지표를 함께 봐야 한다.

- 얼굴 미검출을 위험 신호로 과대 반영하지 않는지
- 중립/정상 상태가 불안으로 치우치지 않는지
- 낙상 점수에 보정값으로 넣었을 때 실제 FP/FN이 줄어드는지
- RTT 증가를 감당할 수 있는지

## 기능 적용 흐름

- 실시간/업로드 분석을 같은 청크 정책으로 맞추는 방향으로 수정했다.
- 실시간 프라이버시 모드에서 skeleton-only 표시와 원본 전환 버튼을 추가했다.
- 업로드 모드에 대량 파일/압축 파일 등록 및 학습 모드 UI를 추가했다.
- 학습 job은 백그라운드로 실행하고 진행률, 처리 개수, ETA, 적용 가능 상태를 표시하도록 구성했다.
- 표정 분석 결과를 행동분석 결과처럼 화면에 표시하는 흐름을 추가했다.
- LLM 판단 근거 생성은 OpenAI API key 설정과 연결했고, 속도 문제 때문에 기본 운영은 local-fast 해석을 우선하도록 조정했다.

## 최근 문제와 대응

### 모델 카드 버전 표시 누락

사용자가 지적한 문제:
- 현재 연결 모델 수치 카드에 active 모델 수치만 있고, 현재 몇 번째 학습 모델인지 보이지 않았다.

대응 방향:
- 백엔드 `model_training_stats`에 `version_badge`, `training_run_label`, `current_training_badge`를 추가한다.
- 프론트 카드에서 모델명 옆에 `vN` 배지를 표시한다.
- 연속 학습 후보가 있으면 `후보 vN` 또는 `학습 중 vN`을 별도 줄로 표시한다.
- 새 백그라운드 학습 job을 시작할 때도 상태 JSON에 `학습 후보 vN`을 저장한다.

### 채팅 재개 실패

사용자에게 안내할 핵심:
- 세션 파일은 살아 있다.
- `/root`와 `/mnt/data/root` 경로 alias가 원인이다.
- 앱 수준에서는 active path로 재개하거나 canonical path 비교를 하도록 고쳐야 한다.
- 프로젝트 관점에서는 이 복원 문서를 기준으로 이어가면 된다.

## 남은 우선순위

1. 현재 연결 모델 카드에 `active vN`, `후보 vN`, `학습 중 vN`이 제대로 보이는지 확인한다.
2. AI-Hub 82 표정 모델은 중립/미검출/불안 쏠림을 추가 검증한다.
3. AI-Hub 173 상태 모델은 F1 0.93 이상 목표로 데이터/전처리/가중치 개선을 지속한다.
4. 가림 보조 모델은 하체 가림과 수직 가림을 분리/통합 비교 후 운영 방식 결정이 필요하다.
5. 표정 모델은 낙상 점수를 직접 뒤집는 주 모델이 아니라, 설명 근거와 confidence 보정값으로 쓰는 것이 현재 가장 안전하다.
