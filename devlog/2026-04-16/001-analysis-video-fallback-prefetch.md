# 분석 상세 부분 영상 로딩 개선 및 청크 분석 병렬화 (I/O Prefetch)

- **ID**: 001
- **날짜**: 2026-04-16
- **유형**: 버그 수정, 성능 개선

## 작업 요약
- 분석 상세 모달의 "부분 영상"(청크 영상) 재생 시, 업로드 로그에 로컬 blob URL이 있으면 이를 최우선으로 사용하고, 없을 경우에만 서버 스트림으로 폴백하도록 개선.
- 서버에서 청크 분석 시, 추론(inference)은 기존대로 순차적으로 진행하되, 다음 청크의 영상 추출(clip extraction) 작업을 백그라운드 스레드에서 미리 수행하여 I/O와 연산을 겹치도록 최적화. (ThreadPoolExecutor 활용)
- 두 개선 모두 기존 기능과 완벽히 호환되며, 에러 발생 시에도 안전하게 폴백 동작함을 확인.

## 변경 파일 목록
- **프론트엔드**
  - `src/app/page.dashboard/view.ts`: 분석 상세 모달에서 부분 영상 재생 시 blob URL 우선 사용, 서버 스트림 폴백 로직 보강
  - `src/app/page.dashboard/view.pug`: UI 변경 없음 (기존 구조 유지)
- **백엔드**
  - `src/model/struct/video_analysis.py`: 청크 분석 루프에서 clip extraction prefetch 로직 추가, inference는 순차 유지
- **테스트/검증**
  - get_errors로 주요 파일 정상 동작 확인
  - devlog.md 및 todo 리스트 최신화

## 상세 내용
- 부분 영상 재생 실패 시에도 서버 스트림으로 안전하게 폴백되며, 업로드 로그에 blob URL이 있으면 즉시 재생되어 대기시간이 대폭 감소함.
- 청크 분석 파이프라인에서 clip extraction을 미리 준비하여, 추론이 끝나는 즉시 다음 청크 분석이 시작되어 전체 분석 시간이 단축됨.
- 모든 변경 사항은 기존 분석 결과 및 UI와 100% 호환됨을 확인.
